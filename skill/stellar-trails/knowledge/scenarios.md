# Scenario Library (L2 Memory)

Auto-grouped L1 patterns by domain.
Entries are created automatically when L1 patterns for the same domain reach ≥5 entries.
Adapted from TencentDB-Agent-Memory L2 Scenario concept.

---

(No scenarios yet — L1 pattern count per domain has not reached threshold of 5.
This file is a placeholder so Knowledge On-Demand Loading references don't break.
Once a domain accumulates 5+ patterns in patterns.md, group them here.)
